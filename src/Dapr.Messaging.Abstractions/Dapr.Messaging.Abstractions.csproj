<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>Dapr.Messaging</RootNamespace>
        <PackageId>Dapr.Messaging.Abstractions</PackageId>
        <Title>Dapr Messaging Abstractions</Title>
        <Description>Provides abstractions for the Dapr Messaging SDK implementation.</Description>
        <!-- This assembly ships bundled inside the Dapr.Messaging NuGet package; it is not published as a standalone package. -->
        <IsPackable>false</IsPackable>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Dapr.Common\Dapr.Common.csproj" />
    </ItemGroup>

</Project>
