using System;
using System.Collections.Generic;
using System.Net;
using System.Threading;
using System.Threading.Tasks;
using Dapr.Client;
using Grpc.Net.Client;
using Microsoft.Extensions.Configuration;
using Moq;
using Shouldly;
using Xunit;
using Autogenerated = Dapr.Client.Autogen.Grpc.v1;

namespace Dapr.Extensions.Configuration.Test;

public class DaprConfigurationStoreProviderTest
{
    [Fact]
    public void TestConfigurationStoreExtension_ThrowsWithNullStore()
    {
        var daprClient = new DaprClientBuilder()
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = new TestHttpClient() })
            .Build();

        Assert.Throws<ArgumentNullException>(() =>
        {
            new ConfigurationBuilder().AddDaprConfigurationStore(null, new List<string>(), daprClient, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestConfigurationStoreExtension_ThrowsWithEmptyStore()
    {
        var daprClient = new DaprClientBuilder()
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = new TestHttpClient() })
            .Build();

        Assert.Throws<ArgumentException>(() =>
        {
            new ConfigurationBuilder().AddDaprConfigurationStore(string.Empty, new List<string>(), daprClient, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestConfigurationStoreExtension_ThrowsWithNullKeys()
    {
        var daprClient = new DaprClientBuilder()
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = new TestHttpClient() })
            .Build();

        Assert.Throws<ArgumentNullException>(() =>
        {
            new ConfigurationBuilder().AddDaprConfigurationStore("configstore", null, daprClient, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestConfigurationStoreExtension_ThrowsWithNullClient()
    {
        Assert.Throws<ArgumentNullException>(() =>
        {
            new ConfigurationBuilder().AddDaprConfigurationStore("configstore", new List<string>(), null, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestSubscribeConfigurationStoreExtension_ThrowsWithNullStore()
    {
        var daprClient = new DaprClientBuilder()
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = new TestHttpClient() })
            .Build();

        Assert.Throws<ArgumentNullException>(() =>
        {
            new ConfigurationBuilder().AddStreamingDaprConfigurationStore(null, new List<string>(), daprClient, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestSubscribeConfigurationStoreExtension_ThrowsWithEmptyStore()
    {
        var daprClient = new DaprClientBuilder()
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = new TestHttpClient() })
            .Build();

        Assert.Throws<ArgumentException>(() =>
        {
            new ConfigurationBuilder().AddStreamingDaprConfigurationStore(string.Empty, new List<string>(), daprClient, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestSubscribeConfigurationStoreExtension_ThrowsWithNullKeys()
    {
        var daprClient = new DaprClientBuilder()
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = new TestHttpClient() })
            .Build();

        Assert.Throws<ArgumentNullException>(() =>
        {
            new ConfigurationBuilder().AddStreamingDaprConfigurationStore("configstore", null, daprClient, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public void TestSubscribeConfigurationStoreExtension_ThrowsWithNullClient()
    {
        Assert.Throws<ArgumentNullException>(() =>
        {
            new ConfigurationBuilder().AddStreamingDaprConfigurationStore("configstore", new List<string>(), null, TimeSpan.FromSeconds(5));
        });
    }

    [Fact]
    public async Task TestConfigurationStoreExtension_ProperlyStoresValues()
    {
        // Sample item.
        var item = new ConfigurationItem("testValue", "v1", null);

        // Configure Client
        var httpClient = new TestHttpClient()
        {
            Handler = async (entry) =>
            {
                var items = new Dictionary<string, ConfigurationItem>();
                items["testKey"] = item;
                await SendResponseWithConfiguration(items, entry);
            }
        };

        var daprClient = new DaprClientBuilder()
            .UseHttpClientFactory(() => httpClient)
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = httpClient })
            .Build();

        var config = new ConfigurationBuilder()
            .AddDaprConfigurationStore("store", new List<string>(), daprClient, TimeSpan.FromSeconds(5))
            .Build();

        await Task.Delay(TimeSpan.FromMilliseconds(500), TestContext.Current.CancellationToken);

        Assert.Equal(item.Value, config["testKey"]);
    }

    [Fact]
    public async Task TestStreamingConfigurationStoreExtension_ProperlyStoresValues()
    {
        // Sample item.
        var item = new ConfigurationItem("testValue", "v1", null);

        // Configure Client
        var httpClient = new TestHttpClient()
        {
            Handler = async (entry) =>
            {
                var items = new Dictionary<string, ConfigurationItem>();
                items["testKey"] = item;
                await SendStreamingResponseWithConfiguration(items, entry);
            }
        };

        var daprClient = new DaprClientBuilder()
            .UseHttpClientFactory(() => httpClient)
            .UseGrpcChannelOptions(new GrpcChannelOptions { HttpClient = httpClient })
            .Build();

        var config = new ConfigurationBuilder()
            .AddStreamingDaprConfigurationStore("store", new List<string>(), daprClient, TimeSpan.FromSeconds(5))
            .Build();

        await Task.Delay(TimeSpan.FromMilliseconds(500), TestContext.Current.CancellationToken);

        Assert.Equal(item.Value, config["testKey"]);
    }

    private async Task SendResponseWithConfiguration(Dictionary<string, ConfigurationItem> items, TestHttpClient.Entry entry)
    {
        var configurationResponse = new Autogenerated.GetConfigurationResponse();
        foreach (var item in items)
        {
            configurationResponse.Items[item.Key] = new Autogenerated.ConfigurationItem()
            {
                Value = item.Value.Value,
                Version = item.Value.Version
            };
        }

        var streamContent = await GrpcUtils.CreateResponseContent(configurationResponse);
        var response = GrpcUtils.CreateResponse(HttpStatusCode.OK, streamContent);
        entry.Completion.SetResult(response);
    }

    [Fact]
    public void TestConfigurationStore_OptionalAndSidecarUnavailable_ReturnsEmptyConfig()
    {
        var daprClient = new Mock<DaprClient>();
        daprClient
            .Setup(c => c.WaitForSidecarAsync(It.IsAny<CancellationToken>()))
            .Returns<CancellationToken>(ct =>
                Task.Delay(Timeout.Infinite, ct));

        var config = new ConfigurationBuilder()
            .AddDaprConfigurationStore("store", new List<string>(), daprClient.Object, TimeSpan.FromSeconds(1), optional: true)
            .Build();

        // Build() should succeed immediately with no values
        config["anyKey"].ShouldBeNull();
    }

    [Fact]
    public async Task TestConfigurationStore_OptionalAndSidecarBecomesAvailable_PopulatesConfig()
    {
        var callCount = 0;

        var daprClient = new Mock<DaprClient>();
        daprClient
            .Setup(c => c.WaitForSidecarAsync(It.IsAny<CancellationToken>()))
            .Returns<CancellationToken>(ct =>
            {
                var current = Interlocked.Increment(ref callCount);
                if (current <= 1)
                {
                    return Task.Delay(Timeout.Infinite, ct);
                }
                return Task.CompletedTask;
            });

        var configResponse = new GetConfigurationResponse(
            new Dictionary<string, ConfigurationItem>
            {
                ["testKey"] = new ConfigurationItem("testValue", "v1", null)
            });

        daprClient
            .Setup(c => c.GetConfiguration(
                "store",
                It.IsAny<IReadOnlyList<string>>(),
                It.IsAny<IReadOnlyDictionary<string, string>>(),
                It.IsAny<CancellationToken>()))
            .ReturnsAsync(configResponse);

        var config = new ConfigurationBuilder()
            .AddDaprConfigurationStore("store", new List<string>(), daprClient.Object, TimeSpan.FromMilliseconds(100), optional: true)
            .Build();

        // Initially empty
        config["testKey"].ShouldBeNull();

        // Wait for the reload token to fire, indicating background load completed
        var reloaded = new TaskCompletionSource<bool>();
        config.GetReloadToken().RegisterChangeCallback(_ => reloaded.TrySetResult(true), null);
        await reloaded.Task.WaitAsync(TimeSpan.FromSeconds(5));

        config["testKey"].ShouldBe("testValue");
    }

    [Fact]
    public async Task TestConfigurationStore_OptionalAndFetchThrowsTransientError_RetriesAndSucceeds()
    {
        var callCount = 0;

        var daprClient = new Mock<DaprClient>();
        daprClient
            .Setup(c => c.WaitForSidecarAsync(It.IsAny<CancellationToken>()))
            .Returns(Task.CompletedTask);

        var configResponse = new GetConfigurationResponse(
            new Dictionary<string, ConfigurationItem>
            {
                ["testKey"] = new ConfigurationItem("testValue", "v1", null)
            });

        daprClient
            .Setup(c => c.GetConfiguration(
                "store",
                It.IsAny<IReadOnlyList<string>>(),
                It.IsAny<IReadOnlyDictionary<string, string>>(),
                It.IsAny<CancellationToken>()))
            .Returns(() =>
            {
                var current = Interlocked.Increment(ref callCount);
                if (current <= 1)
                {
                    throw new DaprException("transient error");
                }
                return Task.FromResult(configResponse);
            });

        var config = new ConfigurationBuilder()
            .AddDaprConfigurationStore("store", new List<string>(), daprClient.Object, TimeSpan.FromMilliseconds(100), optional: true)
            .Build();

        // Initially empty
        config["testKey"].ShouldBeNull();

        // Wait for the reload token to fire after retry succeeds
        var reloaded = new TaskCompletionSource<bool>();
        config.GetReloadToken().RegisterChangeCallback(_ => reloaded.TrySetResult(true), null);
        await reloaded.Task.WaitAsync(TimeSpan.FromSeconds(5));

        config["testKey"].ShouldBe("testValue");
    }

    [Fact]
    public async Task TestConfigurationStore_OptionalAndDisposed_ExitsCleanly()
    {
        var waitCalled = new TaskCompletionSource<bool>();

        var daprClient = new Mock<DaprClient>();
        daprClient
            .Setup(c => c.WaitForSidecarAsync(It.IsAny<CancellationToken>()))
            .Returns<CancellationToken>(ct =>
            {
                waitCalled.TrySetResult(true);
                return Task.Delay(Timeout.Infinite, ct);
            });

        var config = new ConfigurationBuilder()
            .AddDaprConfigurationStore("store", new List<string>(), daprClient.Object, TimeSpan.FromSeconds(1), optional: true)
            .Build();

        // Ensure background task has started
        await waitCalled.Task.WaitAsync(TimeSpan.FromSeconds(5));

        // Dispose the provider to cancel the background task
        (config as IDisposable)?.Dispose();

        // Verify the mock was called with a token that is now cancelled
        daprClient.Verify(c => c.WaitForSidecarAsync(It.IsAny<CancellationToken>()), Times.AtLeastOnce());
        config["anyKey"].ShouldBeNull();
    }

    [Fact]
    public void TestStreamingConfigurationStore_OptionalAndSidecarUnavailable_ReturnsEmptyConfig()
    {
        var daprClient = new Mock<DaprClient>();
        daprClient
            .Setup(c => c.WaitForSidecarAsync(It.IsAny<CancellationToken>()))
            .Returns<CancellationToken>(ct =>
                Task.Delay(Timeout.Infinite, ct));

        var config = new ConfigurationBuilder()
            .AddStreamingDaprConfigurationStore("store", new List<string>(), daprClient.Object, TimeSpan.FromSeconds(1), optional: true)
            .Build();

        config["anyKey"].ShouldBeNull();
    }

    private async Task SendStreamingResponseWithConfiguration(Dictionary<string, ConfigurationItem> items, TestHttpClient.Entry entry)
    {
        var streamResponse = new Autogenerated.SubscribeConfigurationResponse();
        streamResponse.Id = "testId";
        foreach (var item in items)
        {
            streamResponse.Items[item.Key] = new Autogenerated.ConfigurationItem()
            {
                Value = item.Value.Value,
                Version = item.Value.Version
            };
        }

        var streamContent = await GrpcUtils.CreateResponseContent(streamResponse);
        var response = GrpcUtils.CreateResponse(HttpStatusCode.OK, streamContent);
        entry.Completion.SetResult(response);
    }
}
